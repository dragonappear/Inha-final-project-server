spring:
  profiles:
    include: oauth

  session:
    store-type : jdbc
    jdbc.initialize-schema: always

  datasource:
    url:  jdbc:h2:tcp://localhost/~/db/inha
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug