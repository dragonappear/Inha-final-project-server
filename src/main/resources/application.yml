spring:
  profiles:
    active: real,real-db

  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 1000
  servlet:
    multipart:
      maxFileSize : 10MB
      maxRequestSize : 20MB

logging:
  level:
    root: info
    org.hibernate.SQL: info
  file:
    path: logs

